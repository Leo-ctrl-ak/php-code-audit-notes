# Day 1：环境搭建与 HTTP_REFERER 漏洞追踪

> **日期**：2026-09-24
> **靶场**：MacCMS（苹果CMS v10）
> **工具链**：phpStudy · Seay 源代码审计系统 · VS Code · CMD（`findstr`）

---

## 一、环境搭建

**目标**：在本机快速拉起一套可运行 MacCMS 的 PHP 环境，作为后续审计与验证漏洞的靶场。

**步骤**：
1. 安装 **phpStudy**，使用其集成面板一键部署 **Nginx + MySQL + PHP 7.3.4** 组合环境。
2. 下载 MacCMS（苹果CMS v10）源码，放入 phpStudy 的网站根目录。
3. 通过 phpStudy 面板新建站点并绑定本地域名 / 端口，配置好伪静态规则。
4. 浏览器访问安装向导，按提示填写数据库信息，完成 MacCMS 的初始化安装。
5. 进入后台确认站点功能正常，靶场环境搭建完成。

**踩坑记录**：
- PHP 版本需要与 MacCMS v10 的要求匹配，版本过高或过低都可能出现兼容性报错或安装失败。
- 站点根目录、伪静态规则和数据库连接信息三者任一配置错误，都会导致安装向导无法正常进入下一步。

---

## 二、部署后的安全加固

**目的**：MacCMS 等常见 CMS 的后台入口路径是公开且固定的，扫描器可以批量识别。虽然这是本地靶场，但顺手把加固习惯练起来。

**操作**：将后台入口文件 `admin.php` **重命名**为一个不易被猜到的文件名。

**效果**：
- 降低了被自动化扫描器批量识别后台入口的风险。
- 理解了「后台入口改名」属于**成本极低、收益明确**的基础加固手段——它不修复任何漏洞，但能有效抬高攻击者的探测成本。

---

## 三、杀软误报对抗

**问题**：运行 **Seay 源代码审计系统** 时，被 **Windows Defender** 拦截并误报为威胁，工具无法正常启动或扫描。

**原因**：Seay 属于安全审计类工具，其程序行为特征（批量文件遍历、敏感函数特征匹配等）与恶意软件的静态特征存在重叠，因而触发启发式查杀。

**解决**：在 Windows Defender 的「病毒和威胁防护 → 排除项」中，将 Seay 所在的文件夹整体加入**白名单**，工具即可正常运行。

**收获**：安全从业人员需要在「杀软防护」与「工具可用性」之间做权衡，白名单是标准做法，但必须明确排除范围，避免把整个磁盘加入白名单而削弱防护。

---

## 四、漏洞验证结论（真实追踪）

**追踪过程**：
1. 用 `findstr` 追踪 `mac_get_refer`，锁定了 `All.php` 的构造函数。
2. 进一步全局搜索 `_ref` 变量，最终在 `application/common/model/Collect.php` 中发现了**直接使用 `$_SERVER["HTTP_REFERER"]` 且未做过滤**的代码段。
3. 同时在 `application/admin/controller/Make.php` 中发现了直接利用 `$_SERVER["HTTP_REFERER"]` 进行 `mac_jump`（跳转）的逻辑。

**结论与风险评估**：
- `Collect.php` 中存在极大的 SQL 注入风险（Referer 未过滤直接用于数据库操作）。
- `Make.php` 中暴露了「开放重定向」漏洞，可被用于钓鱼攻击。
- 这证明了 `Seay` 的扫描告警并非空穴来风，它是通过对 `$_SERVER` 全局变量的敏感调用做出的合理预警。人工审计的价值在于精准定位到了真正的危险位置。

**修复建议**：
1. 对于 `Collect.php`，必须使用 ThinkPHP 的查询构造器（如 `where()`），避免字符串拼接 SQL。
2. 对于 `Make.php`，跳转前必须对 `HTTP_REFERER` 进行白名单校验（只允许跳转到自己的站点）。

---

## 五、实验截图

![Seay 扫描结果](screenshots/01-seay-scan.png)

*图 1：Seay 对 `$_SERVER` 相关敏感调用的扫描告警*

![源码漏洞调用点](screenshots/02-vulnerable-code.png)

*图 2：`All.php` 中构造函数的调用点（未过滤直接赋值）*

![源码漏洞定义点](screenshots/03-vulnerable-function.png)

*图 3：`common.php` 中 `mac_get_refer()` 函数的定义点（漏洞根源）*

---

## 六、Day 1 小结

| 维度 | 收获 |
| --- | --- |
| 环境 | 用 phpStudy 独立完成 Nginx + MySQL + PHP 环境的部署与 CMS 靶场安装 |
| 加固 | 掌握后台入口改名的低成本加固思路 |
| 工具 | 解决 Seay 被 Defender 误报拦截的问题，学会配置白名单 |
| 方法论 | 从「工具告警」到「人工追数据流」，完成了第一个漏洞的完整研判闭环 |

**下一步计划**：继续对 MacCMS 进行 SQL 注入、文件上传、命令执行等高危漏洞方向的审计，并把每一次追踪过程沉淀成笔记。
